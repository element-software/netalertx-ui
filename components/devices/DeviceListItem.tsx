import { DeviceIcon } from '@/components/dashboard/DeviceIcon';import { Badge } from '@/components/ui/Badge';import { formatDuration } from '@/lib/utils/duration';
export function DeviceListItem({device}:{device:any}){return <a href={`/devices/${encodeURIComponent(device.id)}`} className="flex items-center gap-4 rounded-3xl border border-white/10 bg-white/[.04] p-4"><DeviceIcon type={device.deviceType} online={device.isOnline}/><div className="min-w-0 flex-1"><p className="truncate text-lg font-bold">{device.displayName}</p><p className="text-sm text-slate-400">{device.ipAddress??'No IP'} · {device.macAddress??'No MAC'} · {device.vendor??'Unknown vendor'}</p></div><Badge tone={device.isOnline?'green':'slate'}>{device.status}</Badge><span className="text-sm text-slate-400">{formatDuration(device.connectedDurationSeconds)}</span></a>}

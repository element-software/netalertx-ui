import { Card } from '@/components/ui/Card';import { DeviceIcon } from './DeviceIcon';
export function OnlineDevicesPanel({devices}:{devices:any[]}){return <Card className="min-h-[24rem]"><h2 className="mb-4 text-xl font-bold">Online devices</h2><div className="space-y-3">{devices.filter(d=>d.isOnline).slice(0,8).map(d=><a href={`/devices/${encodeURIComponent(d.id)}`} key={d.id} className="flex items-center gap-3 rounded-2xl bg-white/[.04] p-3"><DeviceIcon type={d.deviceType} online={d.isOnline}/><div className="min-w-0"><p className="truncate font-semibold">{d.displayName}</p><p className="text-sm text-slate-400">{d.ipAddress ?? 'No IP'} · {d.vendor ?? 'Unknown vendor'}</p></div></a>)}</div></Card>}

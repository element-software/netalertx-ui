# Docker

Use `docker compose up --build` after creating `.env`. The compose file exposes NetGlance on port `3030` by default and stores SQLite data in the `netglance-data` volume at `/app/data/netglance.sqlite`.

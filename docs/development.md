# Development

Run `ENABLE_DEMO_MODE=true npm run dev` for a local dashboard with fake devices. The backend is implemented with Next.js route handlers, SQLite, and Server-Sent Events; there is no separate Express/Fastify service.

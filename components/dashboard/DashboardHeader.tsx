'use client';
import { StatusBadge } from './StatusBadge';
export function DashboardHeader({summary}:{summary:any}){return <header className="flex items-center justify-between gap-4"><div><p className="text-sm uppercase tracking-[.45em] text-cyan-200/70">Local network</p><h1 className="text-5xl font-black tracking-tight">{summary.appName}</h1></div><div className="text-right"><div className="mb-2"><StatusBadge status={summary.status}/>{summary.demoMode&&<span className="ml-2 rounded-full bg-violet-400/15 px-3 py-1 text-xs text-violet-100">DEMO MODE</span>}</div><p className="text-sm text-slate-400">Updated {summary.lastSuccessfulUpdate?new Date(summary.lastSuccessfulUpdate).toLocaleTimeString():'never'}</p><p className="text-2xl font-bold">{new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}</p></div></header>}

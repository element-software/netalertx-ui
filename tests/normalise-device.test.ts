import { describe, expect, it } from 'vitest';import { normaliseDevice } from '@/lib/netalertx/normalise-device';
describe('normaliseDevice',()=>{it('maps NetAlertX fields into device model',()=>{const d=normaliseDevice({id:1,dev_Name:'Laptop',dev_MAC:'00-11-22-33-44-55',dev_LastIP:'192.168.1.9',dev_PresentLastScan:'1',dev_Vendor:'Dell'});expect(d.displayName).toBe('Laptop');expect(d.isOnline).toBe(true);expect(d.vendor).toBe('Dell');});it('labels private MAC devices',()=>{const d=normaliseDevice({mac:'6A:11:22:33:44:55',online:true});expect(d.isPrivateMac).toBe(true);expect(d.displayName).toContain('Private MAC');});});

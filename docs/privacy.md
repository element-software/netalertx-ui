# Privacy

NetGlance is designed to run entirely on your local network. It has no cloud backend, accounts, telemetry, analytics, external tracking, external MAC lookup API, or hosted device fingerprinting service. NetAlertX credentials are used only by server-side route handlers and are never sent to the browser.

'use client';import { useMemo, useState } from 'react';import { DeviceListItem } from './DeviceListItem';
export function DeviceList({devices}:{devices:any[]}){const [q,setQ]=useState('');const [filter,setFilter]=useState('all');const rows=useMemo(()=>devices.filter(d=>(filter==='all'||(filter==='online'&&d.isOnline)||(filter==='unknown'&&d.isUnknown)||(filter==='new'&&d.isNew)||(filter==='disconnected'&&!d.isOnline))&&JSON.stringify(d).toLowerCase().includes(q.toLowerCase())),[devices,q,filter]);return <div><div className="mb-5 flex gap-3"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search devices" className="min-w-80 rounded-2xl border border-white/10 bg-slate-950/40 px-4 py-3 outline-none"/>{['all','online','unknown','new','disconnected'].map(f=><button key={f} onClick={()=>setFilter(f)} className={`rounded-2xl px-4 py-2 ${filter===f?'bg-cyan-300 text-slate-950':'bg-white/5'}`}>{f}</button>)}</div><div className="space-y-3">{rows.map(d=><DeviceListItem key={d.id} device={d}/>)}</div></div>}
